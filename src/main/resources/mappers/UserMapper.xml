<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assignment.shoppingbasketserver.dao.UserDao">

    <insert id="insertUser" parameterType="com.assignment.shoppingbasketserver.dto.UserDto">
        insert into user(user_id, user_pw, user_name, user_address)
        values (#{user_id}, #{user_pw}, #{user_name}, #{user_address})
    </insert>

    <select id="selectUser" resultType="com.assignment.shoppingbasketserver.dto.UserDto">
        select *
        from user
        <if test='user_id != null'>
            where 1=1
            and user_id = #{user_id}
        </if>
    </select>
    
    <delete id="deleteUser">
        delete from user where 1=1 and user_no = #{userNo}
    </delete>
</mapper>